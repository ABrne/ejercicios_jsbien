let prompt = require('prompt-sync')();
contador = 1;
let totalvotos=0;
let votos=0;
while (contador<=3) {
    votos= Number(prompt(`Introduce el número de votos recibidos por el candidato ${contador}`));
    totalvotos+=votos;
    voto

}